
{% extends 'default_frame.twig' %}

{% set menus = ['sort_product', 'sort_product_cfg'] %}

{% block title %}商品並び替えプラグイン 動作設定{% endblock %}
{% block sub_title %}{% endblock %}

{% form_theme form 'Form/bootstrap_3_horizontal_layout.html.twig' %}

{% block main %}

    <div class="row" id="aside_wrap">
        <form role="form" name="form1" id="form1" method="post" action="" novalidate enctype="multipart/form-data">
            {{ form_widget(form._token) }}
            <div id="detail_wrap" class="col-md-9">
                {# 設定ボックス #}
                <div id="detail_box" class="box form-horizontal">
                    <div id="detail_box__header" class="box-header">
                        <h3 class="box-title">動作設定</h3>
                    </div><!-- /.box-header -->
                    <div id="detail_box__body" class="box-body">

                        <div class="form-group">
                            <label class="col-sm-4 col-lg-3 control-label">カテゴリー別 並び替え</label>
                            <div class="col-sm-8 col-lg-9 padT07">
                                {{ form_widget(form.category_mode) }}
                                <small style="color:black">
                                    商品カテゴリーごとに並び替えを管理する場合は有効にしてください。<br>
                                    無効の場合は、カテゴリーに関係なく並び替えを管理します。
                                </small>
                            </div>
                        </div>

                    </div><!-- /.box-body -->
                </div><!-- /.box -->

                {# 説明ボックス #}
                <div id="detail_box" class="box form-horizontal">
                    <div id="detail_box__header" class="box-header">
                        <h3 class="box-title">設定のポイント</h3>
                    </div><!-- /.box-header -->
                    <div id="detail_box__body" class="box-body">

                        <div>
                            ・商品の並び順序は、管理メニューの[商品管理] > [<a href="{{ path('plugin_SortProduct') }}">商品並び替え</a>]で設定してください<br>
                            ・商品カテゴリーごとに並び替えを管理する場合は、[カテゴリー別 並び替え]を有効にして、商品カテゴリーごとに並び順序を設定してください<br>
                        </div>

                    </div><!-- /.box-body -->
                </div><!-- /.box -->
            </div><!-- /.col -->

            <div class="col-md-3" id="aside_column">
                <div id="common_box" class="col_inner">
                    <div id="common_button_box" class="box no-header">
                        <div id="common_button_box__body" class="box-body">
                            <div id="common_button_box__insert_button" class="row text-center">
                                <div class="col-sm-6 col-sm-offset-3 col-md-12 col-md-offset-0">
                                    <button type="submit" class="btn btn-primary btn-block btn-lg prevention-btn prevention-mask" >登録</button>
                                </div>
                            </div>
                        </div><!-- /.box-body -->
                    </div><!-- /.box -->

                    {% if Config != null %}
                        <div id="common_date_info_box" class="box no-header">
                            <div id="common_date_info_box__body" class="box-body update-area">
                                <p><svg class="cb cb-clock"> <use xlink:href="#cb-clock" /></svg>登録日：{{ Config.create_date|date_format }}</p>
                                <p><svg class="cb cb-clock"> <use xlink:href="#cb-clock" /></svg>更新日：{{ Config.update_date|date_format }}</p>
                            </div>
                        </div><!-- /.box -->
                    {% endif %}

                </div>
            </div><!-- /.col -->

        </form>
    </div>
{% endblock %}