{#
GtmLite: Google Tag Manager コンテナタグ設置プラグイン
Copyright (C) 2017 Freischtide Inc. All Rights Reserved.
http://freischtide.tumblr.com/

License: see LICENSE.txt
#}
{% extends 'default_frame.twig' %}

{% set menus = ['store', 'plugin'] %}

{% block title %}Google Tag Manager コンテナタグ設置プラグイン{% endblock %}
{% block sub_title %}設定{% endblock %}

{% form_theme form 'Form/bootstrap_3_horizontal_layout.html.twig' %}

{% block main %}
  <form name="form1" id="form1" method="post" action="?">
    {{ form_row(form._token) }}
    <div class="box form-horizontal">
      <div class="box-header">
        <h3 class="box-title">基本設定</h3>
      </div>
      <div class="box-body">
        <div class="form-group">
          {{ form_label(form.tid) }}
          <div class="col-sm-10 {% if form.tid.vars.errors is not empty %}has-error{% endif %}">
            {{ form_widget(form.tid, {attr: {placeholder: 'GTM-xxxxxxx'}}) }}
            <small class="help-block">
              「GTM-xxxxxxx」形式のIDを入力
            </small>
            {{ form_errors(form.tid) }}
          </div>
        </div>
        <div class="form-group">
          {{ form_label(form.tag) }}
          <div class="col-sm-10 {% if form.tag.vars.errors is not empty %}has-error{% endif %}">
            {{ form_widget(form.tag) }}
            <small class="help-block">
              GTMコンテナタグを挿入するかどうかを決定します。自身で設置したコンテナタグや別のプラグインが挿入したコンテナタグを利用する場合は「挿入しない」を選択してください。
            </small>
            {{ form_errors(form.tag) }}
          </div>
        </div>
      </div>
    </div>

    <div class="box form-horizontal">
      <div class="box-header">
        <h3 class="box-title">オプション</h3>
      </div>
      <div class="box-body">
        <div class="form-group">
          {{ form_label(form.optional_events) }}
          <div class="col-sm-10 {% if form.optional_events.vars.errors is not empty %}has-error{% endif %}">
            {{ form_widget(form.optional_events) }}
            <small class="help-block">
              お気に入り、検索結果、商品カテゴリ、商品詳細、カート、購入完了ページへのアクセスを dataLayer.push で通知する
            </small>
            {{ form_errors(form.optional_events) }}
          </div>
        </div>
      </div>
    </div>

    <div class="row btn_area">
      <div class="col-xs-8 col-xs-offset-2 col-sm-4 col-sm-offset-4 text-center">
        <button class="btn btn-primary btn-block btn-lg" onclick="document.form1.submit(); return false;">
          この内容で登録する
        </button>
      </div>
    </div>
  </form>
{% endblock %}
